# STATUS.md — KIZ Super App

What is built, what is broken, what is next. Update this when you finish a feature
or discover a problem.

**Overall: MVP feature-complete, not production-ready.** All planned modules exist
and work in dev. The gaps below are what stands between this and a real deployment.

---

## 1. Built

| Area | State | Notes |
|---|---|---|
| **QR counter check-in / check-out** | Done | Replaces the paper sign-in book at the KIZ counter. Admin creates a **session** (`CheckInSession`: type `check_in`/`check_out`, a **session/year** name, an optional **validity window** (`opensAt`/`closesAt`, editable later without changing the QR token), and an active toggle — one per sem, flipped by the office at move-in/move-out) and prints a QR sheet (`urus-checkin` Sessions tab, `qrcode` encoding `siteUrl("/checkin/<token>")`) with **student instructions** baked in (English + a small **Mandarin** line for the many China-mainland students). Students scan with their own phone → public page `app/checkin/[token]` (whitelisted in `proxy.ts`) → enter their Matric No. (prefilled if already signed in) → **if that matric already has a KIZ account they must sign in first** (identity check; the login callback returns to the same QR page); students with **no account proceed as a walk-in** → confirm their name → **sign digitally** (plain `<canvas>` pad, `components/shared/signature-pad.tsx`, PNG saved via `saveUpload` to `public/uploads/signatures/`) → their **block/room/bed** is revealed, followed by a **"Next: go to Counter 2 (UKM Real Estate)"** panel (collect/return the key) that shows an admin-uploaded **directions image** (App Setting `checkin_directions_image`, uploaded in `urus-checkin`) and a small **Mandarin** line. Only after that do students **without an account** see an **optional** "Want to use the KIZ app?" prompt (prefilled `/daftar`) — registration is never forced. Scans outside the session window are rejected ("hasn't opened yet" / "closed on …"). Records are stored in `CheckInRecord` (matric + name + room **snapshots**, `signedAt` KL via `nowMalaysia()`); duplicate signs for the same session are rejected; no room assigned yet → blocked with a counter message. Admin **Records** tab (`/[role]/urus-checkin` for `superadmin`/`admin_kiz`, read-only `pengetua`) shows **one row per student** merging check-in + check-out (`consolidate()` groups the per-scan rows by matric; the check-out columns stay blank until the move-out session runs), with a signature dialog showing both the check-in and check-out signatures. Filters: **block** / session / search. **Export Excel** (dependency-free `.xlsx` writer in `lib/xlsx.ts` — a **Summary** sheet, an **All students** sheet, and **one worksheet per block**, frozen header + auto-filter), **Export CSV** (`lib/csv` `toCsv`) and **Print report** (A4 sheet including signatures) for the admin file. Sessions are full **CRUD** — create, edit (name/dates), activate/deactivate, and **soft-delete** (a test QR can be removed; already-captured records are kept). Admins can also record a **manual check-in/check-out** on a student's behalf from the Records tab (look up by matric, pick a session — no signature, flagged **Manual** and attributed to the admin). A **check-in/check-out badge** for the current session (latest session per type) is shown in the admin `urus-bilik` **student list** (+ detail dialog) and on the **student dashboard** hero (`getCheckInStatusForMatrics`). Nav entry under admin Approvals. |
| **Student in-app check-in / check-out (no QR)** | Done | Students who already have a KIZ account can now check in or out straight from the app instead of scanning the counter QR. New member page `/[role]/checkin` (`ahli` only; nav item under **Bookings**, plus the mobile `lagi` menu) resolves the **active, in-window `CheckInSession`** server-side, shows the session name/type + validity, and renders the student's assigned room. If nothing is open it shows "No check-in open right now"; if the student has no bed it shows "No room assigned yet" (same rule as the QR flow). Otherwise the student confirms with the shared `SignaturePad` — no matric typing, identity comes from the session. On submit `submitOwnCheckIn` runs the **same transaction core** as the QR path (new internal `recordSignedCheckIn` helper, refactored out of `submitCheckInRecord`): active-intake check, room check, duplicate-sign guard, signature PNG via `saveUpload`, KL `signedAt`. The record lands in the same `CheckInRecord` table with `manualById = null`, so the admin `urus-checkin` Records tab picks it up unchanged and the dashboard status pill updates. No schema change. |
| **Room import assigns beds (first batch)** | Done | The `urus-bilik` CSV import now accepts a **`No. Bilik`** column (full eKolej columns + room). `mapEkolejRows` gained `roomNumber` + a tolerant `splitRoomCode()` (`K18A-101`, `K18A101`, `K18A 101`, `K18A-101 (Bed A)`); a bare `101` without a block is flagged invalid. On confirm, `confirmImport` auto-creates missing `ResidenceBlock`/`ResidenceRoom` (double = A/B beds) and claims the first free bed (A then B in file order), stamping `selectedAt`. Import preview shows the Room column; all rows must carry a room once any does. Rooms created as **double** by default — a single room is simply a twin with only Bed A filled. |
| **Lost & Found report rework** | Done | `hilang` page widened 760 → 1100px (helpdesk style). The **New Report** card now opens with two clear choices — **I Lost Something** / **I Found Something** (info/success tinted cards, meta in `lib/lost-found-meta.ts`) — and the rest of the form only appears once one is picked. Location is now a **KIZ-location dropdown** (residence blocks K18A–K19D, cafeteria, Dewan Sutera, seminar/meeting rooms, surau, laundry, Dapur Siswa, futsal court, sick bay, parcel locker, offices, parking, campus grounds) with an **Other location…** option that reveals a free-text field; the stored `locationFound` keeps the label or the typed text. Reports now require the **date** the item was lost/found (future dates rejected server-side) plus an optional **approx. time** — new nullable `LostFoundItem.happenedDate`/`happenedTime` (`HH:MM`) columns; item cards render "Lost/Found on 10 Sep · around 6:30 PM" via `lostFoundWhenLabel`. Cards widened to a 3-up grid at `md` and now show a schedule row + location row above a hairline-separated reporter footer; rows reported before this change simply show no when-line. |
| **Student/member dashboard revamp (Things to Do + living widgets)** | Done | Member home (`/[role]` for `ahli`/`staf`) rebuilt on the approved mock: hero shows the **full room placement** (`Block K18A · Room 101 · Bed A`) + **Residential Session** (active `Intake.name`) + **roommate name/matric** from the sibling bed — all still gated on `bilik_allocations_published`. New **Things to Do** checklist with a progress bar (`X of Y done`) that flips to a green **"You're all caught up"** panel when nothing is pending. Items: ① room selection (due label from the `SelectionWindow` `closesAt`), ② acknowledge the latest important announcement (new `AnnouncementAcknowledgment` table; button on the pengumuman feed), ③ register your eCard (auto-set on first eCard page view, `users.ecard_registered_at`), ④ accommodation payment (manual, admin-settled — new `eligible_students.accommodation_payment`, toggle in `urus-bilik` student dialog), ⑤ submit check-in documents (upload PDF/photo → `eligible_students.check_in_*`, admin can open the doc). Below the fold a **widget row**: Important Notice (latest `important` announcement), Upcoming at KIZ (new `Event` model — admin CRUD at `/urus-aktiviti`), My Helpdesk Request (latest ticket + status), Emergency Contact (new `ContentItem` kind — admin CRUD at `/urus-kandungan`), Life at KIZ living-guide card (same table, `link`). Widgets self-hide when empty. Quick actions / Your bookings / Latest announcements moved off the dashboard (still in the nav). Full mobile: things-to-do stacks under the hero, widget row goes single-column on phones. |
| Project setup | Done | Next.js 16, MUI v7 design system, Prisma 7 + pg adapter, Docker Postgres. |
| **Full UI/UX redesign** | Done | Every page across all roles rebuilt on MUI v7 (previously Tailwind/shadcn). Tokens + dual light/dark theme in `lib/theme/`, component library in `components/kiz/` (primitives, patterns, shell), one shell for all roles (NavRail + GlassTopBar + BottomNav + MoreSheet + ⌘K CommandPalette + NotificationDrawer). `proxy.ts` at root is the auth guard. |
| **Minimal SaaS visual system** | Done | Re-tokened to a clean, minimalist modern SaaS look (Linear/Vercel/Stripe school): pure-white surfaces, neutral near-black ink, hairline structure instead of shadows, single sans (`Inter`) with tight tracking — no serif. Decoration is soft lavender/sky gradients only (`gradient.hero/.rail/.mesh/.panel`). Sidebar is now a light rail with a gradient wash (the dark-green slab is gone); mobile BottomNav is a flush frosted 5-slot tab bar; MoreSheet is a bottom sheet. Admin "N bookings pending" hero + student eCard are soft-gradient panels. Gradients and glass fall back correctly in dark mode. |
| **Primary colour decided — teal** | Done | `color.brand[*]` in `lib/theme/tokens.ts` swapped from the placeholder neutral ramp to a teal scale (`#0891B2` primary). Re-skins the whole app automatically (no component changes needed) since every component reads from the token. |
| **Sidebar regrouping** | Done | Admin nav (`nav-config.ts`) split the old catch-all "Admin" group into three clearer groups — **Approvals** (facility/guest-house/room/helpdesk requests), **Content** (announcements/facilities/offices), **System** (users/settings) — so the rail reads as a hierarchy instead of one long list. Member groups (Overview/Bookings/Support/Community) unchanged. |
| **Layout/overlap fixes** | Done | Audited every shared shell/list/card primitive and fixed concrete overlap bugs: `StatusChip` dot no longer uses a margin hack that could crowd long labels; `ListRow` trailing cluster wraps instead of colliding with the chevron on narrow screens; notification badge (top bar), "More" tab badge (bottom nav), and room-selection badges (nav rail/drawer) now sit on fixed-size icon wrappers instead of floating on unbounded ones (was bleeding past its button on multi-digit counts / narrow rails); `MetricTile`'s emphasis dot no longer fights baseline alignment with the big number; chat bubble max-width now shrinks when an admin delete button is also present so the row can't overflow at 320–360px; campus-map free-bed badge insets instead of overhanging into neighbouring zones; admin room-card delete button clearance widened; facility/guest-house admin cards standardized on the `radius.cardLg` token and their 2-col detail grid now truncates instead of wrapping unevenly; CSV import-preview "reason" cell truncates with a tooltip instead of stretching row height; `PageHeader` actions no longer force-equal-flex 3+ buttons into uneven wrap rows; helpdesk assignee trailing info now hides until `md` instead of `sm`; announcement feed date pinned with `ml:auto` instead of a `flex:1` spacer that stops working once the row wraps. |
| **Copywriting pass** | Done | Success/error toasts, empty states, and key-moment copy (booking submitted, room selected/released, profile updated, logo updated, login error, admin CRUD toasts) rewritten from formal/corporate tone to a friendlier, lightly fun tone app-wide (e.g. "Yay! Booking sent 🎉", "Inbox zero 🎉", "Nice, bed locked in! 🛏️"). Destructive-action confirmation dialogs (delete facility/user/guest house) were deliberately left as-is — clarity matters more than fun when the action is irreversible. |
| **Label/placeholder overlap fixed globally** | Done | Every `TextField` that set both `label` and `placeholder` (29+ fields across login, user/facility/guest-house/booking/report forms) rendered the floating label unshrunk over the placeholder text on first paint — MUI only shrinks the label on focus/value by default. Fixed once at the theme level: `MuiTextField` now defaults `slotProps.inputLabel.shrink = true` (`lib/theme/theme.ts`), so the label always sits on the outline notch and never collides with a placeholder, app-wide, including future forms. Also widened `SmartTable`'s MUI X DataGrid cell/header horizontal padding (`components/kiz/patterns/smart-table.tsx`) from the library's tight 10px default so table text (e.g. `urus-pengguna` Users table) isn't flush against cell edges. |
| **Bento dashboards + native mobile** | Done | New primitives: `ListGroup`/`ListRow`/`Surface` (`components/kiz/primitives/list-group.tsx`) and `Bento`/`BentoItem`/`MetricTile`/`ActionTile`/`HeroTile` (`components/kiz/patterns/bento.tsx`). Both dashboards rebuilt as 12-col bento mosaics so the fold is dense (hero + metrics side by side). `PageHeader` compacted. Every list surface (my bookings, more, helpdesk ×2, guest house ×2, approval center, parcels ×2) migrated off the duplicated inline card recipe onto `ListRow` — grouped inset cards with internal dividers, iOS Settings style. Mobile gets real native behaviour: no tap-highlight/callout, no overscroll bounce, 56px touch rows, `.scroll-x` snap rails for filter chips. |
| **Mobile sidebar + student dashboard refresh + room-pick reminders** | Done | (a) Mobile gets the full menu back: hamburger in the top bar opens `NavDrawer` (left drawer mirroring the desktop NavRail) so nothing is phone-unreachable; `lagi` menu also gained a "Choose room" entry it was missing. (b) Student dashboard is more app-like: tinted quick-action icon chips (per-item accent/success/warning/danger/info colours), "Resident" hero eyebrow chip, and an editable avatar on the right of the greeting (camera overlay → `/api/upload` → `updateAvatar` action). (c) Room selection: when the window is open, "Choose Room" shows an amber **Open** badge everywhere (rail, mobile drawer, More sheet, `lagi`) and the bottom-nav More tab gets a dot; the student dashboard shows a reminder banner ("Time to pick your room" + close deadline) only for eligible students who haven't picked yet (`getBilikReminder`). (d) eCard page lets the student change their photo in place — same `avatarUrl` drives the dashboard hero, eCard and profile. |
| **Responsive/accessibility audit** | Done | Audited every form, dialog, card, list, and input across all roles for mobile/tablet/desktop layout and large-text robustness. Fixes: facility name + block chip in the admin facility list now shrink/ellipsize properly (`flex:1; minWidth:0`); announcement bodies wrap unbroken long tokens (`overflowWrap: anywhere` on both the member feed and the admin manage list); `BottomNav` height is now `minHeight` so tab labels never clip; `StatusChip` and the global `MuiChip` small size use `minHeight` + vertical label padding so chips grow instead of clipping at large text sizes. Everything else (bento grids, `ListRow`/`Surface`, dialogs, drawer sheets, chat bubbles, `.scroll-x` rails) was verified responsive already. |
| **Hydration hardening** | Done | Eliminated every render-time SSR/CSR divergence that could throw "hydrated but attributes didn't match": the `min` date attribute on the facility/guest-house/announcement forms (was `new Date()` computed inline → now set post-mount), the announcement "New" badge + `urus-bilik` deadline flag (were `Date.now()` state initializers → now mount-guarded), the availability-calendar "today" highlight (mount-guarded), and the student-dashboard greeting (now computed server-side and passed down). Date/time and `Intl` output were verified identical between Node and Chrome; `useMediaQuery`/`useColorScheme` are hydration-safe via `useSyncExternalStore`/undefined-on-hydration. Verified by a headless-Chrome crawl across all routes in desktop + mobile viewports. |
| Auth & profile | Done | Credentials login, JWT session, role routing, profile edit. The proxy reads Auth.js v5's secure and local session-cookie names, so a successful login is recognised before role routing on both HTTPS and localhost. |
| **Demo login accounts** | Done | Login screen has one-click student, Admin KIZ, and Super Admin demo buttons. Re-running the seed resets these three accounts to the active `kiz123` password, so existing stale credentials cannot prevent testing. The seed also repairs their `accountStatus`/`role` back to `active`, so a drifted demo row can't break quick login. Two **stable, permanent** accounts are additionally guaranteed to exist after any seed/deploy (never removed by seed, always repaired to `active`): superadmin `SUPER001` and student `A999999`, both password `kiz123` — use these to test without depending on the one-click buttons. |
| **Student card redesign (UKM card style)** | Done | `KadMayaCard` (`components/shared/kad-maya-card.tsx`) branches by role: `ahli` (student) renders `StudentCardFace` (`components/shared/student-card-face.tsx`) — a fixed 550px-high portrait card (maxWidth 380) in the **UKM residential-college ID Card** style: two logos on top (UKM crest left, KIZ logo right, grouped at the centre 10px apart in 102×94 slots, each falling back to a monogram tile), "KOLEJ IBU ZAIN" main heading (navy-green `brand[900]`), "myKIZ DIGITAL STUDENT CARD" subheading, a green **ACTIVE STUDENT** status badge, a large centred passenger-style photo (150×184, rounded corners + soft shadow), the name in bold uppercase, the **Student ID centred under the name** (no box), a 40%-width hairline divider, then "Block K18A · Room 101 · Bed A" (room line only once the allocation is **published**; session shows regardless) + "Residential Session 2026/2027", then the **bare QR code** (no white container, 90px), and finally **"Valid until <date>"** = room check-in date + 6 months (one semester), hidden when the student has no check-in yet. Card inner padding is 25px top/bottom with fixed element margins; Class/Year was dropped from the card. The **Residential Session line is a superadmin-managed App Setting** (`residential_session`, settable from `urus-tetapan` → Student Card Design → "Residential Session"; falls back to the active `Intake.name` when empty — `getResidentialSession`/`setResidentialSession` in `lib/settings.ts`). The previous boxed details panel, the large green **name bar**, and the admin colour/gradient pickers are all gone — the design form only configures the background + the two card logos + the session. QR generated server-side from matric ID on `/[role]/kad-maya`; room data flows from the bed graph (`getResidentRoomDetail`, publish-gated) which also returns `checkInAt` (latest `CheckInRecord` check-in `signedAt` at the counter); validity computed with `addMonths`/`formatMalaysiaDate` (`lib/timezone.ts`). Admin settings (`urus-tetapan` → "Student Card Design") has: background upload (PNG/JPG, max 4MB) + UKM/KIZ logo upload (SVG allowed, max 2MB) + Residential Session text field + live preview using the real `StudentCardFace`. Card logos stored via `AppSetting` keys `student_card_ukm_logo`/`student_card_kiz_logo`. |
| Kad Maya | Done | Static QR from matric ID. |
| Guest house booking | Done | Booking, approval, check-in/out, manual payment flag. |
| Helpdesk | Done | Tickets, chat thread, assign, close, out-of-hours auto-reply. **Redesigned (Sep 2026):** the member page is now a purpose-built "How can we help?" ask flow instead of a generic form — **mandatory category picker** (9 categories: Accommodation & Room · Maintenance & Repair · Facilities & Booking · Cleanliness & Waste · Internet & Technology · Safety & Security · Payment & Charges · Student Welfare · General Enquiry, each with label/icon/tint/hint in `lib/helpdesk-meta.ts`), a **location step** (segmented "My room" vs "Facility / elsewhere"; My room prefills the student's published block/room and offers the real block list) and an optional detail box (quick questions can now be a one-liner). Requests render as **rich cards** (`#HD-1024`, subject, category · location, "Submitted <date>", status pill, View Request) — ticket reference format is now `HD-####`. **Status lifecycle expanded** from open/in_progress/closed to `submitted → under_review → in_progress → more_info_required → resolved → closed` (Schema `HelpdeskStatus`); admin opening a fresh ticket auto-marks it under review; admin can Take ticket / Ask for more info / Mark resolved / Reopen / Close; a student reply on a resolved or awaiting-info ticket reopens it to in_progress. New **Open/Closed office badge** (`components/shared/office-open-badge.tsx`, pulsing dot when open) sits in the helpdesk header and inside the dashboard's "My Helpdesk Request" widget; when the office is open and the student has no ticket, the dashboard shows an **"Ask the KIZ office"** card instead of hiding helpdesk entirely (supports the anti-counter-queueing goal). An **emergency notice** (`emergency-note.tsx`) at the top of the form warns students not to use it for fires/injuries/security and lists the office-managed emergency numbers (`content_items` kind `emergency_contact`) with `tel:` links. Legacy `open` tickets were remapped to `submitted` on the dev DB. **Bento layout (Sep 2026):** every helpdesk surface now renders on the app-wide 1100px bento grid (12-col `Bento`/`BentoItem`) instead of thin centred 760–900px columns — the member index pairs the emergency notice (span 8) with the office-status tile (span 4), the ask form is full-width with a 3-up category picker, request history tiles 2-up as cards; the admin inbox gets a status-metrics row (To do / Waiting on student / Resolved / Closed) with the To-do and Done groups side by side (8/4); ticket threads sit beside a summary rail (request meta + reporter/assignee) instead of a single skinny chat column. |
| Facility booking | Done | Availability calendar, approval, PDF slip with `KIZ-BKG-NNNN` ref. |
| Announcements | Done | Tags, pinning, scheduling, expiry, attachments, "New" badge. |
| Community chat | Done | Wide two-pane room with reactions, replies, reports, presence + role badges — see "Community Chat redesign". |
| Lost & Found | Done | Report with photo, status lost/found/claimed. |
| **Accommodation Applications** | Done | `bilik` is now a privacy-safe student application flow, not a room picker. Eligible students choose single (admin review), double (same-gender roommate by matric ID), or flexible placement. Only the requested roommate sees race/religion and must explicitly make the pair final; no student sees blocks, floors, rooms, beds, or names during application. No application remains KIV for admin allocation. Physical `Residence*`/`Bed` inventory is retained for final admin allocation after the window. `RoomApplication` records preference and request status; `Bed.occupantId` remains the final allocation source of truth. Admin Students now exposes gender, race, religion, nationality, faculty, merit, flags, application status, and roommate pairing to support allocation. |
| **Room Selection re-labelling** | Done | Student `/ahli/bilik` no longer reads as an *application* (that was completed & approved in e-Kolej). Member nav item renamed **"Accommodation" → "Room Selection"**; page title now **"Choose Your Room Preference"** with a fresh subtitle. A result banner sits at the top — success ("Yay, you've got a place at KIZ!") for students on the accepted list, error ("We're sorry, we couldn't offer you a room this time." + contact KIZ office) otherwise, replacing the old "No accommodation offer found" empty state. Room-type options reworded to **Single Room / Twin-Sharing Room / No Preference**; the twin-sharing helper text now asks for the preferred roommate's matric ID (same gender, must hold an offer, limited profile shown) and a footer note added: preferences are subject to approval & availability, final placement confirmed by the KIZ office. |
| **Room fees on Room Selection** | Done | Admin can set **monthly room fees** (RM, per student) in `urus-bilik` → Cycle setup → new **Room fees** form (Single + Twin-Sharing fields; blank = hidden; a note records the **1-month deposit**). Fees are stored as `AppSetting` (`bilik_room_fee_single` / `bilik_room_fee_double`, read via `getRoomFees()` in `lib/bilik.ts`), streamed into `RoomApplicationState.fees`, and rendered on the student's Single/Twin option cards as `RM X.XX / month` + "Plus a one-month deposit". Hidden until admin enters a value. |
| **Stable Student included in room-selection intake** | Done | The stable test account **A999999** wasn't on the active intake's accepted list, so logging in as it and opening Room Selection showed the "not successful" banner with no form — and no application ever reached the admin's `urus-bilik` Students tab. Added `A999999` to the seed's `eligibleData` (`prisma/seed.ts`) so reseeds include it, and inserted it into the current dev active intake (`eligible_students`) linking the existing account. After this, applying as A999999 persists a `room_applications` row that shows in the admin Students list. |
| **Remove admin accommodation-payment toggle** | Done | The "Accommodation payment" block (payments icon + status chip + Mark paid/unpaid toggle) is removed from the `urus-bilik` student detail dialog — fees are settled externally at the UKM Real Estate counter. Check-in status ("submitted / not submitted" + view document) stays as a slim card pending a redesigned check-in flow. The `setAccommodationPayment` action + `PaymentToggle` component were removed from `urus-bilik-client.tsx`. Note: member surfaces still reference accommodation payment (dashboard "Outstanding accommodation payment" Things-to-Do + its dialog) — revisit when the new check-in flow lands. |
| **Remove accommodation payment + legacy check-in docs (KIV for new flow)** | Done | The member dashboard "Things to Do" no longer lists **Outstanding accommodation payment** or **Submit check-in documents** (there is no payment gateway and no document to upload). Removed: the two todo rows + their dialogs and the `CheckInUploader` from `ahli-home.tsx`; `submitCheckInDocuments` action from `home-actions.ts`; the `payment`/`checkin` todo ids + `dialog`/`docUrl` fields from `HomeTodo` (`lib/dashboard.ts`); the leftover `setAccommodationPayment` action and the accommodation-payment + check-in "submitted / view document" block from the `urus-bilik` student dialog. Dropped the `EligibleStudent.accommodationPayment` column (schema). The `check_in_submitted_at` / `check_in_doc_url` columns were later dropped too — the redesigned counter check-in flow (QR + signature, see the QR counter row above) superseded them; the eCard "Valid until" line now reads the latest `CheckInRecord` check-in `signedAt` via `getResidentRoomDetail.checkInAt`. |
| **Assign anytime, publish gates visibility** | Done | `urus-bilik` workflow change: the office can **assign rooms at any time** (before *and* after the application deadline) from both the Students tab dialog and the Room-inventory "Manage occupants" dialog — the post-deadline-only lock in `adminAssign` was removed. Students **only ever see their room once the allocation is published**: the "Publish room results" button is now **locked until the application period closes** (client-disabled + server-guarded in `setAllocationsPublished`), shown with the closing date/time. Copy updated across the Students tab, both assign dialogs, and the publish section. |
| **Single-applicant assign copy + room-type labels** | Done | Student-detail assign panel now tailors its text: a confirmed double pair keeps the "place both together in a double room" guidance, while everyone else (single / flexible / KIV) gets short copy — "Pick a free bed. If no single room is left, a shared double room is fine." (single applicants may legitimately go into a double room when singles are full). Free-bed dropdowns in the student dialog now tag each bed's room type (`· single` / `· shared double`) so the office can see at a glance. |
| **Role differentiation (admin vs student)** | Done | (a) **"Choose Room" is student-only** — removed from the sidebar/drawer/⌘K/mobile menu for `superadmin`/`admin_kiz`/`pengetua` (`navForRole` filters by `item.roles`; the `lagi` page builds its menu per role). `bilik` page + its actions are `ahli`-gated (non-students are redirected to their own dashboard). Admins manage rooms via `urus-bilik` instead. (b) **Guest house management** — new `GuestHouse` model (name, description, images, price, capacity, max stay, requires-approval). The admin `urus-rumah-tamu` page gained a **Bookings / Guest Houses** tab (searchParams-driven): add/edit/soft-delete guest houses; deleting is refused while a guest house still has active bookings. The member `rumah-tamu` flow now picks a guest house first (per-house availability calendar, `maxDays` and per-house clash checks in `createGHBooking`, guest-house name shown on admin rows and "My Bookings"). (c) **`[role]` segment enforced for every child route** (new `(dashboard)/[role]/layout.tsx` redirects to the session's own segment) — closes old issue #6. (d) **`pengetua` occupancy crash fixed** — occupancy summary extracted to a read-only `lib/bilik.ts` helper; the gated action wraps it, the page uses it directly for `pengetua`. |
| Parcel tracker | Hidden | Module removed from all navigation & shortcuts (nav, quick actions, admin dashboard, `lagi` menu). Route files `/parcel` & `/urus-parcel` kept in place but unreachable via UI. |
| App settings | Done | Logo upload/remove by superadmin/admin_kiz. The uploaded logo doubles as the browser favicon — served dynamically at 512×512 PNG from `/api/app-icon` (falls back to the default `/favicon.ico` when no logo is set). Optional college-photo upload drives the desktop login panel, with a soft gradient overlay and the existing gradient as its empty-state fallback. |
| **Open Graph / link previews** | Done | Shared links now get a rich preview instead of plain text. `app/layout.tsx` metadata carries `openGraph` + `twitter:card=summary_large_image` + `og:image`/`twitter:image` pointing at `siteUrl("/api/opengraph-image")` and a canonical URL. New route `app/api/opengraph-image/route.tsx` (nodejs, dynamic) renders a 1200×630 teal brand card (soft cyan/teal gradient, centred admin-uploaded logo normalised to PNG, "KIZ Super App" + Malay tagline) via `next/og` + sharp; falls back to text-only if no logo is set. Absolute URLs built by `lib/site-url.ts`: prefers `NEXT_PUBLIC_SITE_URL`, then `AUTH_URL` (already `https://mykiz.my` on the server), then localhost. OG base is the shared `metadataBase`. |
| **Administrative Offices module** | Done | New `Office` model + `/pejabat` (all roles) and `/urus-pejabat` (admin). Student page shows an **interactive block panorama** — one wide shot covering both offices, **drag-to-pan** left/right via framer-motion `drag="x"` (no 360 lib), with two labels glued to the image at admin-configurable % positions (left = Pejabat Pentadbiran KIZ, right = Pejabat UKM Real Estate) that pan with the building. Below: two office cards (featured photo, function) opening a photo dialog with clickable gallery thumbnails. Admin `urus-pejabat` edits office name/function + uploads featured/gallery, and edits the panorama (uploads image + two X-position sliders). All uploads go through the shared validated `saveUpload` pipeline (`lib/image-upload.ts`, same one as `/api/upload` and settings) with a 12 MB cap. Labels derive from `Office.name` (single source of truth). Note: local-FS storage caveat (#8) applies. |
| **AR Directory (arrow navigation)** | Done | `/direktori` is a live **camera compass navigator** (`components/shared/ar/ar-navigator.tsx`): pick a destination and a 3D-shaded chevron arrow over the camera feed turns to point at it, with a Leaflet + OpenStreetMap **mini-map** (`ar-minimap.tsx`, bottom-right) showing the walker's position against the destination. Zero paid APIs — pure Web APIs (`getUserMedia`, `Geolocation.watchPosition`, `deviceorientation`/`deviceorientationabsolute` with iOS `requestPermission()`) plus free OSM tiles, SVG arrow rotated per frame in a `requestAnimationFrame` loop (React state untouched → no re-render per frame), low-pass heading smoothing, haversine + bearing helpers in `lib/geo.ts`. No camera/motion sensors (desktop, declined prompts) → falls back to a distance list + Google Maps directions link, shown alongside the "Open camera view" button in one action row rather than a separate scroll-to-the-bottom button. `Destination` model + `DestinationType` enum (`prisma/schema.prisma`) — admin-managed lat/lng pins with an `indoor` flag, a `verified` flag (see below), and a `building` label for rooms in the single-floor admin building. Admin CRUD at `/urus-direktori` (`lib/direktori.ts`; add/edit dialog with live **OpenStreetMap pin preview**, soft delete). **24 destinations seeded** — all 8 residence blocks (K18A–D, K19A–D), K20A, KIZ Student Premier Housing, the admin/hall cluster, and 8 shared facilities (cafeteria, surau, laundry, futsal court, etc.). **Pin verification (Sep 2026):** new `Destination.verified` boolean (default `false`) plus a "GPS verified" toggle in the admin form and an amber **Unverified** badge in the admin list — 13 of the 24 pins now carry real coordinates read directly off named Google Maps pins (long-press → copy), the rest are estimated/interpolated and flagged for follow-up (tracked in Backlog). **UX pass (Sep 2026):** a one-time permission explainer runs before the first camera/location/motion prompt, with a "Try camera again" retry after a decline; the view opens on the destination list/action-row first (camera vs. Google Maps, side by side) instead of dropping straight into the camera; the arrow's distance/walk-time/accuracy-caveat/turn-hint are consolidated into one bottom bar (matching Google's own AR Live View layout) instead of three separate floating pills, which is also what keeps the mini-map's corner clear; a type filter + nearest-first sort was added to the destination picker now that it holds 24 pins instead of 7; a compass-instability hint suggests the figure-eight recalibration wave. AR overlay colours (e.g. `#1A73E8`) are the one deliberate exception to the token rule — they must stay legible over live camera footage. |
| **User management** | Done | New `/urus-pengguna` admin module (superadmin + admin_kiz). Full CRUD on all accounts: add (with password), edit (name/email/phone/role; matric ID immutable), soft-delete (blocks login, keeps history), and **reset password** (generate/copy or type a new one). Guards: only Super Admin can manage/create/delete/demote Super Admin accounts; can't delete yourself; can't delete or demote the last Super Admin. Re-adding a soft-deleted matric ID restores the account. Login already rejects `deletedAt` users. Roles rendered via `StatusChip` (role tones/labels added to `lib/theme/status.ts`). |
| **Facilities directory (Bookable + Shared)** | Done | The member `tempahan-fasiliti` page is now a two-block directory instead of a flat booking grid. Facilities belong to an admin-managed `FacilityCategory` (`FacilityCategory` model + `Facility.categoryId`) whose `section` (`bookable`/`shared`) picks the fixed top-level heading: **Bookable Facilities** ("Spaces that require a reservation before use." → "View & Book") vs **Shared Facilities** / kemudahan umum ("Facilities available to residents without advance booking." → "View Details"/"Learn More"). `Facility.bookable` is kept in sync with the category's section; `Facility.status` (`open`/`coming_soon`) gates booking (a shared or coming-soon space never accepts bookings — enforced in `createFacilityBooking`). Uncategorised legacy facilities fall back to their `bookable` flag and render under a trailing "More Facilities" group. Cards: image, description, block/capacity/fee, `Open`/`Coming soon` chip where relevant, and a CTA per the section+status meta (`lib/facility-meta.ts`). Non-bookable and coming-soon cards open an info dialog (details/photos, no booking) instead of the booking form. Admin `urus-fasiliti` gained a **Facilities / Categories** tab: category CRUD (name, section, display order; delete refused while facilities are assigned; section moves re-sync facility `bookable` flags), plus the facility form/list now carry category + status + section chips and a section filter. Seed catalogs the six category groups and ten demo facilities (Dewan Sutera, Seminar Rooms 1/2, Meeting Room, Futsal Court, Dapur Siswa, Surau, Laundry Room, Parcel Locker, Sick Bay) with statuses/bookable per the copy. |
| **Self-registration + email verification + `staf` role** | Done | Students and staff register themselves (`/daftar`, public). Email domain picks the role: `@siswa.ukm.edu.my` → `ahli`, `@ukm.edu.my` → `staf` (new `Role` value: member experience like a student — resident home with a "Staff" tag — minus `bilik`, and zero `urus-*` access until a superadmin promotes them via user management). Every self-service account must click a **Resend** verification link (`/sahkan`, public) before first sign-in; login shows a distinct "check your inbox / resend" state (`EMAIL_NOT_VERIFIED` error code from `authorize()`). State machine = `AccountStatus` enum on `users` (`unverified` → `pending` → `active`), admin-created accounts default `active` and skip email. Matric ID stays the identity anchor — the eKolej CSV has no email column — and the "tally" is automatic: on email verify, on login (`autoUpgradePendingUser`), and in bulk when an intake is activated (`reconcileIntakeStudents` in `urus-bilik`), the account is unlocked, `EligibleStudent.userId` linked, and the name backfilled from the official list. Non-active accounts get a status wall instead of the app shell (`(dashboard)/layout.tsx` → `PendingGate`). `urus-pengguna` gains an Account-status column + filter and per-row **Activate** (pending) and **Resend email** (unverified) actions. Token links are single-use SHA-256 hashes (raw never stored), 24 h expiry, soft-deleted on use. Professional HTML template embeds the admin-uploaded logo via `/api/app-icon`. Dev fallback: without `RESEND_API_KEY` the link logs to the server console so the flow is testable offline. |
| **Live deployment** | Done | Running on the VPS behind CyberPanel/OpenLiteSpeed at `mykiz.my` (single VPS, PostgreSQL 16, `mykiznext` systemd service on `127.0.0.1:3010`). Deploy = `./deploy.sh` (lint → auto-commit → push to `main`) → GitHub Action → `scripts/remote-deploy.sh` on the server (pull, npm ci, prisma db push, build, restart). `.env` + uploads stay server-side only — the GitHub repo is public. **VPS-only "Invalid URL" 500 root cause (via `journalctl`):** OpenLiteSpeed already sets `X-Forwarded-Proto`/`X-Forwarded-Host` (see `/usr/local/lsws/conf/vhosts/mykiz.my/vhost.conf*`), and `proxy.ts` *also* rewrote those headers — the two layers merged into a comma-joined value (`x-forwarded-host: mykiz.my, https://mykiz.my`), so Auth.js's `createActionURL` crashed on `new URL("https://mykiz.my, https://mykiz.my")` and every Server Action returned 500. **Fix:** `proxy.ts` must NOT touch forwarded headers (reverted to a plain auth guard). `AUTH_URL=https://mykiz.my` is set in `.env` (enforced idempotently by `remote-deploy.sh`) so Auth.js uses the explicit origin, and `experimental.serverActions.allowedOrigins: ["mykiz.my", "*.mykiz.my", "www.mykiz.my"]` handles the CSRF origin check (Next 16 honours it — `action-handler.js` `isCsrfOriginAllowed`). |
| **VPS build-hang fix (memory) + CI typecheck** | Done | The Sep-2026 deploy hung at `Creating an optimized production build …` for 29 min until the SSH timeout, then OOM'd at `Running TypeScript`. **Root cause:** the VPS has **1.9 GB RAM + 2 GB swap**, and the new `googleapis` dependency (210 MB / ~1900 files) was being bundled by Turbopack, then Next's build-time TS check needed >1 GB heap (default limit ~1008 MB on this box). **Fixes:** (a) `serverExternalPackages: ["sharp", "googleapis"]` in `next.config.ts` — googleapis only runs in the `urus-bilik` server action, so it's required at runtime instead of bundled (compile dropped 30-min hang → 44 s); (b) `typescript.ignoreBuildErrors: true` + a new **`typecheck` job** in `.github/workflows/deploy.yml` that runs `prisma generate` → `next typegen` → `tsc --noEmit` on the 7 GB GitHub runner before the deploy job (`needs: typecheck`) — the VPS build now only compiles/prerenders. Deploy is ~2 min end-to-end again. |
| **Migrated to a dedicated VPS (nginx + PG18)** | Done | Moved off the shared CyberPanel/OpenLiteSpeed box (Ubuntu 24.04, PG16) to a dedicated **Ubuntu 26.04** VPS (`43.156.60.15`): **nginx** reverse-proxy → `127.0.0.1:3010` (`mykiznext` systemd, user `mykiz7808`), **PostgreSQL 18**, Node 22, SSL via **Let's Encrypt DNS-01** (Cloudflare token at `/root/.secrets/cloudflare.ini`, auto-renew). Data moved with `pg_dump`/`pg_restore` + `rsync` of `.env` and `public/uploads/`; DNS A records (`mykiz.my`, `www`) re-pointed in Cloudflare (proxied); GitHub secrets `DEPLOY_HOST`/`DEPLOY_USER` updated, `DEPLOY_KEY` reused. **Why:** the old 2 GB box shared RAM with CyberPanel + other sites + swap was 1.2 GB used; the new dedicated box idles ~650 MB used / 1.3 GB available. nginx is intentional (no control panel) — Cloudflare handles DNS/SSL UI. Old VPS left running as fallback, to be decommissioned manually. |
| **Deploy speedup (fail-fast + skip npm ci)** | Done | Deploy felt like 10–15 min because a bad VPS build hung at `Creating an optimized production build …` until the SSH `command_timeout: 30m` (a 14 Sep run burned the full 30 min), and `concurrency: group: deploy, cancel-in-progress: false` made every later push queue behind it. Also `npm ci` ran on both the runner and the VPS on every deploy even when deps hadn't changed. **Fixes:** (a) `timeout-minutes: 15` on both jobs + `command_timeout: 8m` so a hang fails in minutes instead of 30; (b) `scripts/remote-deploy.sh` now only runs `npm ci` when `package-lock.json`'s sha256 differs from the last successful install (hash cached in `$APP/.deploy-lock-hash`, `--prefer-offline`); (c) a `flock -w 300` guard serializes deploys on the host so a cancelled/orphaned run can't race a new one on `node_modules`/`.next`. `cancel-in-progress` deliberately left `false` (GitHub already cancels superseded pending runs; cancelling mid-deploy risks an orphaned remote process). Happy-path deploy stays ~2.5–3 min (typecheck ~55s + SSH ~100s: npm ci 44s + build 36s + prisma/restart 16s). |
| **App Settings upload fix — 10 MB proxy body cap** | Done | App Settings uploads (login background / dashboard banner / dashboard poster) failed on the live server with the generic "Upload didn't go through" for any file over ~10 MB, while every other upload surface worked. **Root cause:** Next 16 clones each request body that passes through `proxy.ts` (middleware) to hand the proxy a readable stream, and that clone is capped by `experimental.proxyClientMaxBodySize` — **default 10 MB**. Settings uploads are Server Actions (page POSTs → they hit the proxy) and advertise up to **12 MB**, so anything over 10 MB was silently truncated to the first 10 MB before the action ran (`Request body exceeded 10MB for /superadmin/urus-tetapan` in `journalctl`). `/api/upload` is excluded from the proxy matcher, which is why facility/announcement/avatar uploads never hit it. **Fix:** `experimental.proxyClientMaxBodySize: "20mb"` in `next.config.ts` (above the 16 MB `serverActions.bodySizeLimit`). Also un-swallowed the real error in the background/hero/poster client catches so future failures show the actual message instead of the opaque fallback. |
| **Canonical room format (`K18A-101 (Bed A)`)** | Done | All residence room/bed displays now use one format: a room code is the full `K18A-101` (block · floor · 2-digit room) and a twin bed is shown as `(Bed A)` / `(Bed B)`; single rooms show only the code. Pure helpers in `lib/bilik-format.ts` (`roomCode`, `parseRoomNumber`, `bedLabel`, `bedWord`, `roomAssignmentLabel`) are the only place this is formatted. Members read their room from the **bed graph**, not the denormalised `users.block`/`room_number` columns: `getResidentRoomLabel()` resolves `EligibleStudent.bed → room.block` and is **gated on `allocations_published`** for home, eCard, `lagi`, profile and the bilik page — nothing is shown until the KIZ office assigns *and* publishes. Admin views (helpdesk ticket, user list) read the same graph without the publish gate. Profile no longer lets students type their own block/room. Dynamic-inventory safety, enforced in Server Actions: a block whose beds are occupied can't change gender or be renamed; a room with occupants can't be set to maintenance/closed. Room entry now accepts `K18A-101` or `101` and always stores the full code (floor derived). Backfill script `prisma/backfill-rooms.ts` normalises legacy room numbers. No schema migration needed — `BedPosition` stays `single|left|right`, mapped to A/B only at display. |
| Dark mode | Removed | App is **light-only** — the `dark` color scheme, top-bar theme toggle, and all `[data-mui-color-scheme='dark']` overrides were stripped; theme/provider/`InitColorSchemeScript` pinned to `light` (`lib/theme/theme.ts`, `components/providers/app-providers.tsx`, `app/layout.tsx`, `glass-top-bar.tsx`). |
| UI language | Done | Interface is English. URL slugs kept Malaysian (`pengumuman`, `tempahan`, etc.) by design; not converted to avoid breaking links. Seed `umum/penting/aktiviti` tags changed to English form values (`general`/`important`/`event`); dates localized to `en-MY`. |
| **Announcement feed redesign (grid + reactions + unread)** | Done | The member `pengumuman` feed is rebuilt from a plain single-column list into a **responsive 2-up card grid** (container widened 820 → 1100px). Each card shows: **category colour + icon** tile (general/important/event/sports meta in `lib/announcement-meta.ts` — tinted chips and filter icons), a **3-line clamped summary** (replaces the too-short one-liners), publish date, pinned state, a **green "New/Unread" pill that clears once the announcement is opened** (ahli/staf only), resident **reaction counts 👍 Noted / ❤️ Excited / 🙋 Interested**, and a **"View details →"** affordance. The whole card is clickable (hover lift + arrow slide) and opens a **full-reading dialog** (`announcement-dialog.tsx`) with the category header, timestamp, poster, full content, attachment and the reaction bar. Backed by two new tables: `AnnouncementReaction` (unique `(userId, announcementId, type)`, soft delete) and `AnnouncementRead` (unique `(userId, announcementId)`). For **important** announcements, tapping 👍 Noted upserts the existing `AnnouncementAcknowledgment` — and un-noting clears it — so the dashboard "Acknowledge important announcement" Things-to-Do task is driven by the reaction bar; the old separate "Mark as acknowledged" button was removed. Subtitle changed to "Stay updated with the latest news and notices from KIZ." Seed now adds sample reactions/reads for the demo student + peers. |
| **UI/UX consistency audit round 1** | Done | Cross-app audit + standardization sweep. **Global:** `theme.shape.borderRadius` reverted to MUI's default `4` multiplier — numeric `borderRadius` in sx was scaling by 10 (theme had `shape.borderRadius = radius.button`), so values like `2`/`2.5`/`3`/`28` silently rendered 20/25/30/280px; numeric radii now mean conventional MUI multipliers and branded radii are px-string tokens. **Chat:** all three threads (community `chat-room`, helpdesk `ticket-chat`, admin `admin-ticket-chat`) unified onto one bubble recipe — neutral `action.hover` tint for the other party (was blue `info` in helpdesk), 16px asymmetric corners, sender label *above* the bubble on others' messages only, caption timestamp under every bubble (helpdesk had none), same `px 1.5/py 1/fontSize 14`, `gap 1.5`, `maxWidth 80/70%`, same hairline composer + round send button, `radius.card` surface. **Destructive/activate confirm dialogs:** 7 files (`delete-button`, `delete-guest-house`, `delete-event`, `delete-content`, `delete-destination`, `delete-user`, `activate-user`) used `<p style={{ color: "text.secondary" }}>` — an invalid CSS color that silently rendered near-black — now `Typography variant="body2" color="text.secondary"`. **Shell:** MoreSheet top corners `borderTop*Radius: 28` (→280px) → `radius.sheet`; sheet + drawer + notification panel titles unified to `h3`; "Open" room badge font/px unified across rail/drawer/sheet/`lagi`; nav logo corners pinned to 4px. **Dashboards/lists:** `ahli-home` todo rows get `py 1.25–1.5` + `minHeight 48–52` touch rows and the room-placement chip is now a pill like its session neighbour; home-widget icon tiles normalized to `radius.input`. **Admin tables:** `urus-bilik` Students tab stat bento now fills all 12 cols (`span 3`) and its raw table got SmartTable-consistent uppercase micro-headers + row hover + `radius.card`; `urus-pengguna` action column widened (152→176) with even gaps and right-aligned icons. **Member screens:** `tempahan-fasiliti` gained a `PageHeader`, facility cards standardised on `radius.cardLg` + 20px content padding, the booking form is capped at 640/860 measure with a back affordance into the grid and token radii on imagery; `kad-maya` photo surface + QR tip pinned to the card's 380px measure with token radius; legacy eCard outer radius → `radius.cardLg`. **Helpdesk:** office-hours banner now hairline-bordered on `radius.cardLg`, the unread count is a real pill tag (not a button), the section label matches the group-heading scale, and the empty state uses `KEmpty`. **Dialog forms:** `event-form`, `content-form`, `destination-form` dropped `size="small"` so all admin dialog fields share the default height. Lint + `tsc --noEmit` + production build all green. Known follow-ups (round 2) listed in Backlog. |
| **UI/UX consistency round 1b — admin search + table density** | Done | Search/filter inputs across every admin (and member facilities) list were `size="small"` (≈40px) next to default-height form fields — promoted all `FilterBar` search fields and the `urus-bilik` student/inventory searches to the default input height so toolbars read the same size as the forms they sit above. `SmartTable` (urus-pengguna Users table etc.) defaulted to MUI `density="compact"` (≈40px rows) — flipped the default to `"standard"`, raised cell/header horizontal padding 14→16px and cell type `0.8125rem → 0.875rem`, so rows and columns breathe. `urus-bilik` block-name / room-number dialog fields also de-`small`'d. Lint green on all touched files. |
| **Community Chat redesign** | Done | The single-room chat is rebuilt as a wide, purpose-built community room. **Layout:** page widened (1280px) into a two-pane layout — chat on the left, a **Community info rail** on the right (stacked under it on phones / opened as a right drawer from the header ⓘ). **Header** shows member count + live online count ("X members · Y online" + Live/Idle dot), driven by a **presence heartbeat**: `users.lastSeenAt` is stamped by the client every 15s (`touchChatPresence`), and "online" = seen within `ONLINE_WINDOW_MS` (2 min). **Messages** now show avatar + name + **role badge** (Resident/Staff/Fellow/Principal/Admin KIZ/Super Admin via `chatRoleBadge` in `lib/chat-meta.ts`) + time, with day separators. **Reactions:** emoji chips under each bubble (👍❤️😂😮😢🙏), tap to toggle — new `ChatMessageReaction` table (unique user×message×emoji, soft delete). **Reply:** an inline quote thread — new `replyToId` self-relation on `CommunityChatMessage`; the composer shows a "Replying to …" strip and bubbles render a quoted preview of the parent. **Report:** each message can be reported with a preset reason + note — new `ChatMessageReport` table; a KIZ-team flag button in the header opens a moderation dialog listing open reports with **Delete message / Dismiss** actions (`dismissChatReport`), and deleting a message auto-closes its reports. **Composer:** image/PDF attachment (uploaded via `/api/upload?dir=chat` → `/uploads/chat`), an in-house curated emoji menu (no new library), and a clearer raised round send button. **Pinned guidelines** strip sits at the top of the room, and the right rail shows the full **Community Guidelines** (static copy in `lib/chat-meta.ts`), the **Community Team** (office + fellows from `users`, with online dots), and a card pointing official matters/complaints to **Helpdesk**. Community team + guidelines are *not* admin-editable yet (static copy / derived from accounts). Seed now adds a demo `fellow` account (`FEL001`, password `kiz123`) and, into an empty room, a 6-message welcome thread with reactions. |
| **Chat sender profile peek (admin)** | Done | `admin_kiz`/`superadmin` can click a message sender's name/avatar to open a `UserProfileDialog` showing avatar, name, role badge, online dot, matric ID, email, phone, allocated room (via `getResidentRoomLabel`, admin view), account status, and member since. Students see no clickable affordance and the `getChatUserProfile` server action is role-guarded, so non-admins can't pull another resident's details. |
| **New `fellow` role** | Done | `Role` enum gained `fellow` (residential college fellow). Behaviour = **member like `staf`**: resident-style home with a "Fellow" tag (`AhliHome` hero), chat/announcements/helpdesk/eCard/bookings/lost&found all work, no `bilik`, no `urus-*` access. Added to `lib/rbac.ts` (`MEMBER_ROLES`/`isMemberRole`), nav `ROLE_LABELS`/`ROLE_OVERLINES`, `welcomeMessages`, all `requireMember()` guards (home-actions, pengumuman), `kad-maya`/`rumah-tamu`/`pengumuman` is-member checks, `status.ts` role tone/label, the `urus-pengguna` role picker + filter chips, and the seed (`FEL001`). Not reachable via self-registration (admin-created only). |
| **Real accepted-list import (KIZ layout)** | Done | The `urus-bilik` CSV import now reads the actual KIZ/eKolej export: separate **`BLOCK` + `ROOM`** columns (combined into `K18A-101`), `COUNTRY`→nationality, `FAC`→faculty, and **no gender column** — gender is derived from the block via `BLOCK_GENDER_MAP` (K18A/K19A/K19B male; K18B/C/D/K19C/D female; `lib/room-selection.ts`). Room type is inferred from the file: **one bed row = single, two = twin**. Rows whose `NAME` starts with `ROSAK` → room **maintenance**, `BILIK GANTIAN`/`KUARINTIN`/`KEGUNAAN LAIN` → **closed** (flagged rooms are created with no occupant); blank bed rows (BLOCK+ROOM present, no name/matric) are **empty** and skipped, so a partial twin keeps its empty partner bed. A row with a name but **no matric** (e.g. `PENGETUA`, `FKAB MOBILITY`, `UKM Global`, `UKMRE-PETRONAS`) is a **non-student resident** — skipped, and a room with only such rows is not created (existing residents left untouched). Note: in the 4 rooms mixing a student + a non-student resident, the student is placed and the non-student's bed is left as a free bed (the app has no non-student occupant model). `mapEkolejRows` now returns per-room type/status + a `groupMappedRooms` grouping pass; `previewImport` reports **students / rooms / flagged rooms / invalid / empty / non-student occupants**; `confirmImport` creates every room with its type + status and assigns beds to matric rows in file order, skipping matrics already in the active intake. The file is treated as **authoritative for the rooms it lists**: any stale occupant (demo data / a previous intake) and old reserve flag in those rooms is released first, then the room's type/status is reconciled — so re-imports never get stuck on "already occupied" or a single/twin mismatch (the success toast reports how many old allocations were released). Still CSV-only (export the sheet first — the dropzone rejects `.xlsx` with a hint). A local dev DB's demo residence data can be wiped with `npx tsx prisma/reset-bilik.ts --yes`. |
| **Room selection closed for placed students** | Done | Once a student has a bed (`EligibleStudent.bed`), the `/ahli/bilik` page renders a **"Room selection is closed"** state instead of the application form (`getApplicationState` now returns `hasRoom`; `room-application-flow.tsx` short-circuits), the dashboard room reminder is suppressed (`getBilikReminder` returns `show: false`), and every `bilik` Server Action rejects with "Room selection is closed — you already have a room assigned." (`studentContext` guard). |
| **Room type fix + reserved beds** | Done | Admins can now **change a room's type** (single ↔ twin) straight from the Room inventory card (`updateRoomType`): a single room with a student becomes a twin with the student in Bed A; a twin only converts to single when at most one bed is occupied. Existing bed rows are revived/soft-deleted (never hard-deleted) so the `(roomId, position)` unique constraint is always satisfied. Non-student residents in the accepted list (`PENGETUA`, `FKAB MOBILITY`, `UKM Global`, `UKMRE-PETRONAS`, `PBSM`, `UKM SHAPE`, …) are no longer skipped: their beds get a new **`Bed.reserved`** flag (emergency / pengetua quota / staff). A room with only reserved beds is created **closed**; a mixed room stays available with the student placed and the partner bed reserved. Reserved beds are excluded from free/assignable occupancy (new `getOccupancySummary.reserved` tile), cannot be claimed by import (`claimBed`), admin assignment (`adminAssign`) or the student picker, and render as **Reserved** in the inventory card + occupants dialog. Schema: `beds.reserved Boolean @default(false)` — run `npx prisma db push`. |
| **Google Sheets sync (in-place)** | Done | `urus-bilik` → Cycle setup now has a **Sync** section beside the one-shot Import. Sync **updates the active intake in place** (no new intake): the sheet is diffed against the DB by matric — new matric → **added**, changed room → **moved**, matric gone → bed **released** (record kept, `selectedAt` cleared) — and every room's type/status/reserved beds are reconciled. Students whose room is **unchanged keep their bed and `selectedAt`** (only profile fields refresh), so a no-op sync touches nothing. A **Preview** shows add/move/release + rooms new/changed before anything is applied; apply is idempotent (verified on the real 437-student sheet). Source is either the configured **Google Sheet** (service account, **values-only** — colours aren't needed — scope `spreadsheets.readonly`) or a CSV upload for testing. Config (`google_service_account` JSON, `google_sheet_id`, `google_sheet_range`) lives in `app_settings` with `.env` fallback; the key is never returned to the client. Core diff/apply is in `lib/bilik-sync.ts`, shared bed helpers in `lib/bilik-rooms.ts` (both plain, testable). Import + sync transactions use a **120s timeout** (they touch ~360 rooms). New dep: `googleapis`. |
| **KIZ-AI concierge (Gemini RAG)** | Done | The app now has an AI layer, `lib/ai/` — **zero new npm deps**: `provider.ts` calls the Gemini REST API via `fetch` (text + strict-JSON modes), `embed.ts` embeds via `text-embedding-004` and compares with cosine similarity in TS, `rag.ts` indexes app content into a flat `AiKnowledge` table (embedding stored as JSON — no pgvector) and retrieves top-k, `prompts.ts` holds the guardrailed system prompts, `config.ts` reads the key/model from `app_settings` with a `GEMINI_API_KEY` env fallback. The robot **KIZ-AI** (admin-uploaded mascot image + name) appears as a floating bottom-right launcher (framer-motion float animation, hidden when AI is off), a right drawer on desktop / bottom sheet on mobile, and entries in the top bar + ⌘K palette. The mascot supports **3 emotions × 3 frames** (`idle`/`thinking`/`happy`, uploaded in `urus-tetapan`, downscaled to 256px PNG with alpha preserved via `sharp`, stored as JSON in `AppSetting` `concierge_frames`): the launcher + header crossfade-loop the frames (idle 520ms, thinking 240ms, happy 300ms), the robot switches to **thinking** while the model runs and flashes **happy** on a good answer, and falls back to a single static image then the sparkle icon. Chat-bubble avatars stay static to avoid noise. Answers cite source chips linking to the relevant page; when nothing relevant is indexed it says so and offers a one-tap **"connect to the office"** handoff. Indexing is idempotent (sha256 skip-unchanged, soft-delete removed). Admin config + **Re-index** + the **"questions KIZ-AI couldn't answer"** feedback list live in `urus-tetapan` → KIZ-AI (Gemini). Schema: `ai_knowledge`, `ai_unanswered_log` — run `npx prisma db push`. |
| **Helpdesk split: Live Chat vs Support Ticket** | Done | `HelpdeskTicket` gained a `channel` enum (`live` / `ticket`) and an `origin` field (`web` / `concierge`). The member Helpdesk page now offers **Live Chat** (a quick-question composer, no category form → `startLiveChat`) above **Support Ticket** (the existing categorised, tracked form). Both share the same thread, inbox, assign/close actions and out-of-hours auto-reply; the admin inbox (`urus-helpdesk`) gets **Tickets / Live Chat** tabs. New `room_change` helpdesk category for formal room-move applications. Tickets that arrived via KIZ-AI show a "via KIZ-AI" hint in the admin list. |
| **Helpdesk AI assist** | Done | In the admin ticket view, an **AI assist** button asks Gemini to categorise, prioritise (low/normal/high/urgent), summarise and **draft a reply** from the ticket thread (`analyzeTicket`, strict-JSON via `responseSchema`). The draft is inserted into the composer for the admin to edit — never auto-sent — and the suggested category can be applied with one click. Admin-only (`requireRole`), human-in-the-loop. |
| **KIZ-AI dual provider (Gemini + Ollama) + BM25 fallback** | Done | Chat and embeddings are now **independently configurable** in `urus-tetapan` → KIZ-AI: each can use **Google Gemini** or a local **Ollama** server (OpenAI-compatible `/v1/chat/completions` + `/v1/embeddings`), or embeddings can be disabled. `lib/ai/provider.ts` and `embed.ts` dispatch by provider (`ai_chat_provider` / `ai_embed_provider`; Ollama URL + models are settings). **Retrieval** (`ai_retrieval_mode`: auto/embeddings/keyword) uses cosine similarity when embeddings are available and **falls back to BM25 keyword search** (`lib/ai/bm25.ts`, pure TS) when they aren't — so RAG still works if the embedding provider hits quota/billing/offline, and indexing stores content even when embedding fails. A **Test connection** button pings chat + embeddings and reports the exact error. Default Gemini embedding model upgraded from the retired `text-embedding-004` to `gemini-embedding-001` (saved values auto-upgrade). |
| **FAQ knowledge base + dedicated AI admin menu** | Done | New `Faq` table (`category`, `question`, `answer`, `keywords`, `language`, `published`) is the primary "training" surface for KIZ-AI — no fine-tuning. Admin `urus-faq` (`superadmin`/`admin_kiz`) does full CRUD with category filter/search, a **publish/draft** toggle, **import** (accepts `.xlsx` or `.csv`; matches on question so re-importing updates answers instead of duplicating), **CSV export**, and **"Add starter questions"**, and **"Sync from Google Sheet"** (`lib/ai/faq-sheet.ts`) — staff edit a shared sheet and the admin pulls it on demand (no re-uploading). The FAQ sync shares the accommodation service account (`google_service_account`) but has its own `faq_sheet_id` / `faq_sheet_range` settings; the reader detects the header row (the one containing `question`) so a title above the table is fine. The setup steps are shown in-app as a collapsible guide. The downloadable **Excel template** (`lib/ai/faq-template.ts`, built with the in-house `buildXlsx` writer — no new deps) has a **"Panduan"** sheet (Malay instructions, examples, category list) and a **"FAQ"** sheet to fill in: **82 KIZ-procedure questions in Bahasa Melayu** (`lib/ai/faq-seed.ts`) with blank answers and English/Chinese `keywords` for cross-language matching. Uploads are parsed server-side by `lib/xlsx-read.ts` (minimal ZIP+DEFLATE reader via `node:zlib`). Published + answered FAQs are indexed into `ai_knowledge` by `collectSources()` (title = question, content = answer + keywords). The **"questions KIZ-AI couldn't answer"** list is embedded on the page with a one-click **Add FAQ** that pre-fills the form — the feedback loop. The admin sidebar now has a dedicated **AI** group (**KIZ-AI** = providers/robot/knowledge/test; **FAQ Knowledge**) so AI config no longer lives inside App Settings; `urus-ai` was split out of `urus-tetapan`. KIZ-AI replies in the asker's language (the system prompt already enforces this). Schema: `faqs` — run `npx prisma db push`. |
| **Helpdesk live-chat translation (zh ⇄ en) + staff identity** | Done | (a) **Admin/member split fixed** — the resident ask flow (`/[role]/helpdesk`) now **redirects `admin_kiz`/`superadmin` to the inbox** (`/[role]/urus-helpdesk`), and the "Helpdesk" nav item is member-only (`roles: ahli/staf/fellow/pengetua`), so the office never lands on the student UI by mistake. (b) **Staff name on replies** — the admin thread (`admin-ticket-chat.tsx`) now renders **every** sender's name + role badge above the bubble (previously only the student's name showed; staff replies were anonymous). One account per staff member (created via `urus-pengguna`) is all that's needed — no schema change. (c) **Live-chat translation, both ways** — `HelpdeskMessage` gained `sourceLang` / `translationEn` / `translationZh` (`npx prisma db push`). On send, `lib/helpdesk-translate.ts` schedules a best-effort translation **after the response** (`after()` from `next/server`, so chat stays instant) using the existing AI provider (`lib/ai/translate.ts`, Gemini/Ollama — **no new dependency**); the original text is always kept. Only `channel = live` threads translate — Support Tickets are left as typed. **The office always reads/writes English** (no admin-side translation controls); the **resident gets an English / 中文 switcher** on their thread (defaulting to Mandarin only when their first message is detected as `zh`), with a "Translated to …" hint under translated bubbles. The admin inbox preview shows the English translation. `getTicketMessages` also gained a participant guard (staff or the ticket owner only). `translateMessage` returns `null` when AI is off/fails, so everything degrades to the untranslated message. |
| **Invitation emails (superadmin)** | Done | New `/urus-jemputan` (nav under **System**, `roles: ["superadmin"]` only). A superadmin invites people to self-register, **one at a time or in bulk** — a textarea takes one person per line (`email, matric No., full name`, matric/name optional) and a role picker offers **Student** or **Admin KIZ**. Each recipient gets a professional HTML email (admin-uploaded logo via `/api/app-icon`, "KOLEJ IBU ZAIN" header) with an **Accept invitation** CTA to `/daftar?invite=<token>`; the raw token is never stored (SHA-256 only), expires in **14 days**, single-use. The invited email + role are locked from the invitation, so registration **bypasses the UKM-domain check** (admin staff can be invited on any address). For a student, if the matric matches the **active intake** the invitation is flagged **Resident** (shown in the table + email) and registering + verifying the email activates and links the account straight away; admin invitations never need intake matching. The manage table shows recipient/role/Resident/status (**Pending · Accepted · Expired · Revoked**)/invited-by/sent-count/expiry with **Resend** (re-issues the token), **Revoke**, and soft-**Delete** per row. Schema: new `Invitation` model + `User.invitationsSent`/`invitationsAccepted`; `registerAccount` gained `inviteToken`; `verifyEmailToken`/`autoUpgradePendingUser` now treat every non-`ahli` role as active (previously `staf` only). Run `npx prisma db push`. |

---

## 2. Known issues

Ordered roughly by severity. None of these are fixed — they are recorded so you
don't rediscover them or build on top of them.

### #0b — ~~Student dashboard 500s after the dashboard revamp~~ FIXED

Two causes during the member-dashboard revamp: (1) a running `next dev` kept the
**old generated Prisma client** in memory, so `lib/dashboard.ts`'s
`user.findUnique({ select: { ecardRegisteredAt } })` threw
`PrismaClientValidationError: Unknown field ecardRegisteredAt` → 500 on `/ahli`.
Fix = `npx prisma generate` + restart the dev server (also `db push` first for the
new columns/tables). (2) `kad-maya/page.tsx` called **`revalidatePath` during a
server-component render**, which Next.js forbids ("used revalidatePath … during
render which is unsupported") → 500 on `/ahli/kad-maya`. Moved the eCard
registration write into a client component (`components/shared/ecard-registration.tsx`)
that fires `markEcardRegistered()` (Server Action, revalidates outside render)
after mount.

### #0c — ~~My Bookings (`/[role]/tempahan`) 500s for every user~~ FIXED

The server component passed `<Button component={Link}>` elements as the
`actions` prop to the `PageHeader` client component. `Link` is a function, and a
function prop on an element crossing the server→client boundary can't be
serialized — React threw `Functions cannot be passed directly to Client
Components…` (digest `4202328819`) → the default "This page couldn't load / A
server error occurred" page. Because the header actions render before the
`total === 0` check, the empty state ("Nothing booked yet") was never reachable;
every user, with or without bookings, hit the 500. Fix: moved the two header
buttons into a small `"use client"` `MyBookingsHeaderActions` component so the
`component={Link}` props are created on the client, and passed that as `actions`.
While there, the row date/time formatting was given `timeZone: "Asia/Kuala_Lumpur"`
(it was rendering in the server's timezone). Rule of thumb: never pass an element
that carries a **function prop** (e.g. `component={Link}`) as a prop to a client
component — define it inside a client component instead.

### #1 — ~~`middleware.ts` in the wrong directory~~ FIXED

Moved to `proxy.ts` at the repo root (Next.js 16 `proxy` convention) and rewritten
to use `getToken` from `next-auth/jwt` so it runs on the Edge runtime without
pulling Prisma in. It now actually guards protected routes and redirects logged-in
users away from `/login`. Role-per-route enforcement still lives in each page
(`auth()` + redirect) and each mutation (`requireRole()`).

### #1b — ~~`/ahli/bilik` locks page scroll after selecting a room~~ FIXED

On desktop the mobile bottom-sheet `Drawer` in `room-picker.tsx` was always mounted
with `open={Boolean(openRoom)}`, hidden only via `sx display:{md:none}`. MUI's
`ModalManager` sets `body.style.overflow = 'hidden'` whenever a modal is open —
regardless of CSS visibility — so clicking any room card (which drives the desktop
side panel) invisibly locked body scroll and the page couldn't scroll. Fixed by
gating the drawer on `useMediaQuery(theme.breakpoints.down('md'))`; on desktop it
never opens, on mobile it opens/closes normally and releases the lock.

### #2 — ~~Duplicate facility booking systems~~ FIXED

Legacy `tempahan/` + `urus-tempahan/` removed. `tempahan-fasiliti/` +
`urus-tempahan-fasiliti/` are the single facility-booking pair, and `tempahan/`
now hosts the merged "My Bookings" timeline.

### #3 — ~~Parcel module hidden behind "coming soon"~~ FIXED

`/parcel` member page now renders live parcel data (was a hardcoded `comingSoon`
banner). Nav labels updated.

### #4 — `app_settings` has no `deletedAt` (medium)

The only table missing soft-delete columns — it also lacks `createdAt`. Violates
the project-wide rule in `AGENTS.md`. Low practical impact (it is a key/value
store) but it is an inconsistency.

### #5 — ~~`/api/upload` does not validate uploads~~ FIXED

All image uploads are now routed through one shared, validated pipeline —
`lib/image-upload.ts` (`saveUpload`). The upload is sniffed by content (via
`sharp`, never trusted `file.type`), must **fully decode** before anything is
written, and is capped by size everywhere. Web-safe rasters (jpeg/png/webp/gif)
pass through unchanged; exotic phone/camera formats (HEIC/HEIF, TIFF, BMP, AVIF)
are re-encoded to jpeg/png so the stored file always renders in a browser
preview — this fixed intermittent "corrupt / broken image" uploads on the live
server (iPhone HEIC uploads were being stored raw). Corrupt/truncated uploads
are now rejected with a clear message instead of being persisted. Adopted by
`/api/upload` (now images + PDF, 15 MB cap), `lib/settings.ts` (logo 2 MB + SVG,
student-card bg 4 MB), `lib/offices.ts` (12 MB), and lost & found photos.

### #6 — ~~Child routes don't re-check the `[role]` segment~~ FIXED

`[role]/page.tsx` verifies that the URL role matches the session role, but nested
routes did not. Added `(dashboard)/[role]/layout.tsx` which validates the `[role]`
segment for every child route and redirects to the session's own segment, so a
student loading `/admin_kiz/...` is bounced to `/ahli/...` immediately.

### #7 — RBAC is applied inconsistently (medium)

`AGENTS.md` says use `requireRole()`. In practice most `actions.ts` files do inline
`session.user.role` checks, and `urus-tetapan` does its own check plus a redirect
instead of using the helper. Same outcome today, but each inline copy is a place to
forget a role.

### #8 — Local filesystem storage won't survive deployment (blocker for launch)

All uploads (facility images, announcement attachments, lost & found photos,
booking PDFs, app logo) are written to `public/uploads/`. On a stateless host such
as Vercel these vanish on every deploy and are not shared between instances.

*Status:* partially resolved for the single-VPS deploy — the app now lives in
`/home/mykiz.my/kiznext` on a persistent volume, so uploads survive deploys.
Still not shared across instances; revisit object storage only if we ever scale
out.

### #9 — Room selection: CSV-only import, no block editor for single-room work (low)

The `urus-bilik` Building tab now has a full block/floor/room editor: add a block
(name, gender, floors, sort order), add a single room (block, floor, number,
single/double), bulk-generate a floor, rename a block (dialog), and soft-delete
blocks/rooms (refuses when a room still has an occupant). Import is still
**CSV only** by design (no `.xlsx` — no spreadsheet lib in the stack); admins
export the eKolej sheet to CSV first. `nationality` is defaulted to `Malaysia`
because the eKolej column list has no nationality field. The campus map zones in
the student picker are still a code-defined percentage layout — a new block
won't appear on the map image until its zone is added (the map is a fallback;
the grid picker picks it up automatically).

### #10 — Kad Maya QR is static and unsigned (low)

The QR encodes the matric ID as plain text. Anyone who knows a matric ID can
generate an identical code. Fine for casual identification, not for access control.

### #11 — AR Directory needs real-device calibration (low)

The compass code path is best-effort across browsers (iOS `webkitCompassHeading`
vs Android `deviceorientationabsolute` `alpha`), and the seeded destination pins
are first-pass estimates spread across the real KIZ footprint. Both should be
verified on a physical phone and re-pinned in `urus-direktori` from Google Maps
("What's here?" → copy lat/lng). GPS indoors is coarse (±5–10 m), so distance
readouts for indoor rooms (Dewan Sutera, etc.) are directional hints only. The
arrow is now a Google-Street-View-style **standing marker** — projected to the
horizontal screen position of its bearing via an assumed camera FOV
(`HALF_FOV = 34°` half-angle in `components/shared/ar/ar-navigator.tsx`), sliding
as you turn and growing/dropping as you approach. The assumed FOV is a guess;
if the pin sits too far to one side when centred ahead (or vice-versa), tune
`HALF_FOV` against a real phone. True world-anchored 3D (as opposed to this
directional projection) would need camera-pose tracking — out of scope for a
compass-only web app.

---

## 3. Backlog

Not started, roughly in priority order.

- **Production hardening** — resolve #4, #8 before any real deployment.
- **UI/UX consistency round 2 (audit findings not yet fixed)** — per-screen gaps recorded in the audit: migrate the `urus-bilik` Students raw `Table` to `SmartTable` (mobile card fallback); replace native `confirm()`/`alert()` flows (`urus-bilik` room delete + bulk status, `cancel-gh-button`, announcement edit-delete) with the `KDialog` confirm pattern; unify auth cards (login 360 vs register 400 width, footer treatment) and gate the demo-login CTAs; normalise `size="small"` vs default buttons/touch targets on the remaining row actions (parcel collect, ticket close, chat delete, AR destination chips, avatar camera badge); extract a shared `TagChip` (three hand-rolled brand tag pills) and a shared bottom-nav/MoreSheet `NavRow` so the two mobile surfaces can't drift again; standardise remaining numeric-radius leftovers and any raw hex/`#fff` on filled chips to tokens; re-skin the AR-navigator fallback header off the retired inline tinted-tile recipe; `pending-gate` custom button/KIcon bypasses. `urus-bilik` hand-rolled dialogs and filter pills are still ad-hoc rather than `KDialog`/`FilterBar`.
- **`admin_ukmre` role** — external guest-house operator. Add the enum value and
  route guest-house approvals to it; no schema restructure needed.
- **`audit_logs` table** — actor, action, target type/id, JSON meta. Was specced
  but never built; worth having before admins act on real student data.
- **`notifications` table** — generic in-app notifications. Parcel arrival, booking
  approval, and announcement posts currently notify nobody. The notification
  drawer is currently an empty shell.
- **Reports page for `pengetua`** — view-only analytics surfaced from the dashboard
  (bookings per week, ticket status mix). MUI X Charts are installed and ready.
- **Dynamic / signed QR** — addresses #9.
- **Tests** — no test framework is set up at all.
- **AR Directory: 3 destinations still Unverified** — `Blok K18B`, `Dapur
  Siswa`, `Parcel Locker` — coordinates are interpolated/estimated, not
  confirmed against a real device or Google Maps pin. (`Blok K19D`, `Pejabat
  Pentadbiran KIZ`, `Cafeteria`, `Futsal Court`, `Sick Bay`, `Pejabat UKM Real
  Estate`, and `Meeting Room` were fixed with real on-device coordinates
  2026-09-18, on top of the 13 fixed 2026-09-16. `Bilik Seminar` turned out to
  be two distinct bookable rooms — split into `Seminar Room 1`/`Seminar Room
  2`, both real-pinned, matching the Facility Booking system's existing
  "Seminar Room 1"/"Seminar Room 2".) Confirm each remaining one on a real
  device (the "Capture GPS here" button in `/urus-direktori` while standing at
  the real spot is the fastest way) or via Google Maps, then toggle "GPS
  verified" on.
- **AR mini-map hotlinks OpenStreetMap's free public tile servers** —
  `components/shared/ar/ar-minimap.tsx` pulls tiles straight from
  `tile.openstreetmap.org`, which explicitly discourages production-scale
  traffic. Fine for a single residential college's user base today; revisit
  (switch to a managed provider like MapTiler/Stadia Maps/Geoapify, or throttle
  tile refetches) only if usage grows enough for OSM to start rate-limiting
  the app's IP, which would silently break just the mini-map (arrow/camera/GPS
  keep working either way).

---

## 4. Decisions settled

Recorded so nobody reopens them. All of these were once marked "to decide" and are
now resolved in code.

| Question | Decision |
|---|---|
| Realtime engine | None. Client polling every 3s. No Pusher/Supabase. |
| Auth provider | Auth.js v5 (next-auth beta), Credentials provider. |
| Auth email | Resend (SDK `resend`). Key + sender address are configurable in **App Settings (`urus-tetapan` → "Email (Resend)")** and stored in `app_settings` (server-only, key is masked in the UI and never sent back to the browser); `RESEND_API_KEY`/`RESEND_FROM` in the server `.env` act as fallback when unset. Template is inline-HTML (no react-email dep), logo embedded from `/api/app-icon`. Registration identity anchor is **matric ID** — the eKolej CSV has no email column, so the email is an ownership gate + contact, not a join key. |
| Auth guard | `proxy.ts` at repo root (`getToken` + JWT), per-page `auth()` + redirect, per-mutation `requireRole()`. |
| File storage | Local filesystem `public/uploads/`. Provisional — see #8. |
| ORM | Prisma 7, `prisma-client` generator, `@prisma/adapter-pg`. |
| UI / design system | MUI v7 heavily customized; tokens in `lib/theme/`; light-only via CSS variables; components in `components/kiz/`. |
| Parcel notification channel | In-app only. No email/SMS/push. |
| Task tracking | This file. The Telegram Director Bot and `TASKS.md` were removed. |
| Hosting | Live on a dedicated VPS (**Ubuntu 26.04**, IP `43.156.60.15`) at `mykiz.my` — **nginx** reverse-proxy (port 80/443) → `127.0.0.1:3010` (`mykiznext` systemd service, runs as `mykiz7808`), **PostgreSQL 18** on the same box, SSL via **Let's Encrypt DNS-01** (Cloudflare API token in `/root/.secrets/cloudflare.ini`, auto-renew), DNS proxied through Cloudflare. Deploy via GitHub Actions (`DEPLOY_HOST=43.156.60.15`, `DEPLOY_USER=ubuntu`, `DEPLOY_KEY` = `kiznext-deploy`). Run `./deploy.sh` locally: lint → auto-commit → push → action SSHes in, `scripts/remote-deploy.sh` pulls + rebuilds + restarts the service. `.env`/uploads live only on the server (repo is public). Migrated from the old shared CyberPanel box (Ubuntu 24.04, PG16, OLS) on 14 Sep 2026. |

services:
  db:
    image: postgres:16-alpine
    container_name: kiznext-db
    environment:
      POSTGRES_USER: kiznext
      POSTGRES_PASSWORD: kiznext_dev
      POSTGRES_DB: kiznext
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
